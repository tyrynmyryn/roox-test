import React from 'react';
import s from './Loader.module.scss';

const Loader = () => {
    return (
        <div>
            <div className={s.loader}/>
        </div>
    );
};

export default Loader;